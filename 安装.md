# macOS 安装 Rust 和 Snowcap 开发环境

不包含 GNS3/FRRouting runtime case study 的部署步骤。

## 1. 安装 Xcode 命令行工具

```bash
xcode-select --install
```

安装完成后可验证：

```bash
cc --version
```

## 2. 安装 Homebrew

如果尚未安装 Homebrew，先安装：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Apple Silicon 机器一般会安装到 `/opt/homebrew`。安装后确认：

```bash
brew --version
```

## 3. 安装 Rust

官方推荐使用 `rustup` 管理 Rust 工具链：

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

安装完成后让当前终端生效：

```bash
source "$HOME/.cargo/env"
```

验证：

```bash
rustc --version
cargo --version
rustup --version
```

安装常用组件：

```bash
rustup default stable
rustup component add rust-src rustfmt clippy
```

## 4. 如果 rustup 下载很慢

可以临时使用 USTC 镜像安装 Rust：

```bash
export RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static
export RUSTUP_UPDATE_ROOT=https://mirrors.ustc.edu.cn/rust-static/rustup

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

如果希望后续 `rustup update` 也走镜像，把下面内容写入 `~/.zshrc` 或 `~/.bashrc`，我是写入到`~/.bash_profile`：

```bash
export RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static
export RUSTUP_UPDATE_ROOT=https://mirrors.ustc.edu.cn/rust-static/rustup
```

## 5. 安装 Snowcap 编译依赖

`snowcap_main` 会间接依赖 OpenSSL、libpcap 和 pkg-config：

```bash
brew install pkgconf openssl@3 libpcap
```

配置当前 shell 的链接环境：

```bash
export OPENSSL_DIR="/opt/homebrew/opt/openssl@3"
export PKG_CONFIG_PATH="/opt/homebrew/opt/openssl@3/lib/pkgconfig:/opt/homebrew/opt/libpcap/lib/pkgconfig:$PKG_CONFIG_PATH"
```

建议将这两行也写入 `~/.zshrc` 或 `~/.bashrc`，我是写入到`~/.bash_profile`。

## 6. 配置 Cargo 下载源

如果 Cargo 下载 crate 速度正常，可以不配置镜像。

如果国内网络下载很慢，可以使用 USTC crates.io 镜像：

```bash
mkdir -p ~/.cargo

cat > ~/.cargo/config.toml <<'EOF'
[net]
git-fetch-with-cli = true

[source.crates-io]
replace-with = "ustc"

[source.ustc]
registry = "sparse+https://mirrors.ustc.edu.cn/crates.io-index/"
EOF
```

注意：不要把 Cargo registry 写成裸域名，例如 `https://rsproxy.cn`，否则可能出现 404。

## 7. 构建项目

进入项目根目录：

```bash
cd /Users/liml/RustroverProjects/snowcap
```

先做轻量检查：

```bash
cargo check -p snowcap_main
```

构建 release 版本：

```bash
cargo build -p snowcap_main --release
```

## 8. 验证运行

查看命令帮助：

```bash
./target/release/snowcap_main --help
```

运行一个最小 example：

```bash
RUST_LOG=info ./target/release/snowcap_main synthesize example simple-net
```

运行一个 Topology Zoo 场景：

```bash
RUST_LOG=info ./target/release/snowcap_main synthesize \
  topology-zoo eval_sigcomm2021/topology_zoo/HiberniaIreland.gml FM2RR
```

## 9. 不需要安装的内容

如果目标只是跑算法、改代码和调试 `snowcap_main`，暂时不需要：

- Docker
- GNS3
- FRRouting
- KVM/虚拟化
- 论文全量评估依赖

这些只在复现论文 artifact 或运行真实 GNS3 runtime case study 时才需要。
